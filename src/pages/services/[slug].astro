---
const legacyRedirects = {
  'ai-workshop': '/services/t1-sales-skills',
  'ai-workshop-advance': '/services/t3-sales-back-office',
  'paid-audit': '/services/daily-consulting',
  'package-a': '/services/daily-consulting',
  'sales-system-sprint': '/services/daily-consulting',
  'sale-training-bundle': '/services/t1-sales-skills',
  'trust-content-tiktok-workshop': '/services/online-to-sales',
} as const;

export function getStaticPaths() {
  return [
    'ai-workshop',
    'ai-workshop-advance',
    'paid-audit',
    'package-a',
    'sales-system-sprint',
    'sale-training-bundle',
    'trust-content-tiktok-workshop',
  ].map((slug) => ({ params: { slug } }));
}

const { slug } = Astro.params;
const destination = legacyRedirects[slug as keyof typeof legacyRedirects] ?? '/services';
return Astro.redirect(destination, 301);
---
