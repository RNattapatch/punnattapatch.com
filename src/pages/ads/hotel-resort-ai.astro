---
import BaseLayout from '../../layouts/BaseLayout.astro';
import DarumaMark from '../../components/DarumaMark.astro';
import AdsContentV2 from '../../components/ads/AdsContentV2.astro';
import { SITE } from '../../data/site';
import { PRICES, fmtPrice } from '../../data/pricing.mjs';

const title = 'อบรม AI ให้ทีมโรงแรม & รีสอร์ท · คุยฟรี';
const description = 'แขกทักมาตอนตีสอง กว่าจะตอบตอนเช้าเขาจองที่อื่นไปแล้ว อบรมในองค์กร 1 วันที่โรงแรมของคุณเอง ทีมสร้าง AI Agent จากข้อมูลจริงของที่พัก คุยฟรี 15–20 นาที';
const canonical = `${SITE.url}/ads/hotel-resort-ai`;
const lineUrl = 'https://line.me/R/ti/p/@011xgvap';
const perHead = Math.round(PRICES['inhouse-a'].amount / 20);

const content = {
  pageId: 'hotel',
  formAnchor: '#hotel-ai-form',
  lineUrl,
  capacity: 'ผมสอนเองทุกรุ่น รับสูงสุดเดือนละ 10 บริษัท',
  capacityLeft: 'เดือนสิงหาคมเหลือ 4 บริษัท',

  eyebrow: 'คอร์สอบรม In-house · โรงแรมและรีสอร์ท · ทีม 5–20 คน · 1 วันเต็ม',
  headlineLead: 'ลดงานตอบซ้ำ',
  headlineNowrap: 'คืนเวลาทีม',
  headlineAccent: 'ไปดูแลแขกตรงหน้า',
  productName: 'Advance AI Agent สำหรับโรงแรม',
  productTagline: 'คอร์สลงมือสร้าง AI Agent ที่รู้จักห้องพัก ราคา และแพ็กเกจของคุณ จบใน 1 วัน',
  lede: 'ทีมไม่ได้กลับพร้อมแค่สไลด์หรือ prompt ครับ ทั้ง Front, Reservation และ Sales สร้าง AI Agent ของตัวเองจากข้อมูลจริงของที่พัก ทั้งประเภทห้อง ราคาแต่ละฤดู และเงื่อนไขการจอง พร้อมกติกาให้คนตรวจก่อนตอบแขก',
  badges: ['ทีม 5–20 คน', 'จัดที่โรงแรมคุณ', `ราคาเดียวทั้งทีม ${fmtPrice('inhouse-a')}`],
  ctaLabel: 'คุยเรื่องทีมของคุณ ฟรี 15 นาที',
  ctaNote: 'คุยเพื่อเช็กความเหมาะสมก่อนครับ ถ้าทีมยังไม่พร้อม ผมบอกตรงๆ',

  heroPhotos: {
    main: { src: '/lp/inhouse/hero-pointing.jpg', alt: 'ปันยืนชี้อธิบายบนจอหน้าห้อง ขณะทีมลงมือทำบนเครื่องตัวเองในคลาสจริง', width: 1600, height: 1200 },
    left: { src: '/lp/inhouse/hands-on.jpg', alt: 'ทีมนั่งทำงานกับเครื่องตัวเองระหว่างเวิร์คช็อป', width: 1600, height: 1200 },
    right: { src: '/lp/inhouse/team-selfie.jpg', alt: 'ปันถ่ายรูปกับทีมผู้เรียนเต็มห้องหลังจบคลาส', width: 1200, height: 1600 },
  },
  heroCaption: 'ภาพจากคลาสจริง · ผู้เรียนลงมือบนเครื่องตัวเอง',

  clientLogos: [
    { src: '/logos/clients/scenery-farm.jpeg', alt: 'The Scenery สวนผึ้ง' },
    { src: '/logos/clients/farevefarm.jpg', alt: 'Farevefarm' },
    { src: '/logos/clients/farmsuk.jpg', alt: 'ฟาร์มสุข farmsuk' },
    { src: '/logos/clients/nissan.png', alt: 'Nissan' },
    { src: '/logos/clients/futureskill.png', alt: 'FutureSkill' },
    { src: '/logos/clients/hfc-healthfoods.png', alt: 'HFC HealthFoods Corporation' },
    { src: '/logos/clients/home-plus.png', alt: 'ฮ.โฮมพลัส' },
    { src: '/logos/clients/ving.png', alt: 'V!NG' },
    { src: '/logos/clients/business-boy.jpg', alt: 'เด็กประกอบการ The Business Boy' },
    { src: '/logos/clients/aes.jpg', alt: 'AES' },
  ],

  proofHeadline: 'องค์กรชั้นนำ และแบรนด์ดังไว้วางใจ',
  proofBody: 'รวมถึงที่พักและฟาร์มสเตย์ที่ผมเข้าไปอบรมให้ถึงหน้างานจริง',
  perHeadNote: `มากัน 20 คน เฉลี่ยคนละ ${perHead.toLocaleString('th-TH')} บาท ถูกกว่าส่งพนักงานไปเรียนข้างนอก 1 ที่นั่ง`,

  calloutLead: 'คืนที่แล้วมีแขกทักเข้ามากี่ข้อความครับ',
  calloutAccent: 'แล้วตอบทันกี่ข้อความ?',
  calloutBody: 'คำตอบที่ผมได้ยินบ่อยที่สุดคือ "เช้ามาก็ตอบไปแล้วนะ แต่เขาไม่ตอบกลับ" ห้องที่ขายไม่ออกของโรงแรมส่วนใหญ่ไม่ได้หายตอนแขกต่อราคาครับ มันหายตอนไม่มีใครตอบตอนตีสองต่างหาก',
  pains: [
    { icon: 'moon', text: 'แขกต่างชาติทักตอนตีสอง กว่าจะตอบตอนเช้า เขาจองที่อื่นไปแล้ว' },
    { icon: 'repeat', text: 'คำถามเดิมวนซ้ำทุกวัน ห้องแบบไหนต่างกันยังไง มีรถรับไหม เช็คอินกี่โมง ทีมพิมพ์ตอบเองทุกครั้ง' },
    { icon: 'brain', text: 'ราคาแต่ละฤดูกับเงื่อนไขแพ็กเกจ อยู่ในหัวคนเก่งคนเดียว เขาลาทีวุ่นทั้งโรงแรม' },
    { icon: 'star', text: 'รีวิวแย่โผล่มาทีหลัง ไม่รู้ว่าเรื่องไหนพังซ้ำๆ กว่าจะรู้ดาวก็ตกแล้ว' },
    { icon: 'wallet', text: 'กรุ๊ปทัวร์กับลูกค้าองค์กรถามใบเสนอราคา กว่าจะทำเสร็จเขาก็ได้ที่อื่นไปแล้ว' },
  ],
  painsClose: 'ถ้าอ่านแล้วนึกหน้าทีมหน้าเคาน์เตอร์ออกทีละข้อ ผมเข้าใจครับ ทีมไม่ได้ทำงานช้า แต่คนคนเดียวตอบแขกสิบคนพร้อมกันตอนดึก มันไม่ไหวจริงๆ',

  whyTitle: 'ทำไมผมถึงยกคลาสไปที่โรงแรมคุณ แทนที่จะให้ทีมเดินทางมาเรียน',
  whyCols: [
    { heading: 'สอนด้วยห้องพักและราคาของคุณเอง', body: 'ตัวอย่างในคลาสคือประเภทห้องจริง ราคาแต่ละฤดูจริง แพ็กเกจจริง agent ที่สร้างเสร็จจึงตอบแขกได้ทันที ไม่ต้องมานั่งแปลงเอง' },
    { heading: 'ทั้งทีมขยับพร้อมกัน', body: 'ส่งไปเรียนคนเดียว กลับมาเปลี่ยนทั้งโรงแรมไม่ได้ครับ แต่ถ้า Front, Reservation และ Sales เห็นของจริงพร้อมกันในวันเดียว วันถัดไปทุกคนตอบแขกด้วยข้อมูลชุดเดียวกัน' },
    { heading: 'โรงแรมไม่ต้องหยุดให้บริการ', body: 'ไม่ต้องพาทีมเดินทาง ใครติดแขกที่เคาน์เตอร์ก็ออกไปดูแลแล้วกลับเข้าห้องได้ และราคาเดียวทั้งทีม ถูกกว่าซื้อคอร์สรายหัวหลายเท่า' },
  ],
  principleQuote: '"AI เหมือนน้องฝึกงานที่รู้ทุกอย่างบนโลก แต่ยังไม่รู้จักโรงแรมคุณ"',
  principleBody: 'วันแรกของทีมคุณจึงต้องเริ่มจากข้อมูลห้องพัก ราคา และเงื่อนไขการจองของที่พักเอง ไม่ใช่เริ่มจากท่องจำ prompt นี่คือสิ่งที่คอร์ส AI ทั่วไปข้ามไป แล้วมางงว่าทำไมทีมเลิกใช้',

  agendaTitle: 'หนึ่งวันเดินจาก "งานหน้าเคาน์เตอร์" ไปหา "Agent" ทุกช่วงมีชิ้นงานออก',
  agendaLead: 'วงกลมข้างเวลาคือดารุมะของทีมคุณครับ เราแต้มตาให้มันช่วงท้ายวัน เมื่อ agent ของทุกคนมีกติกาใช้จริงครบ',
  agenda: [
    { time: '09:00', until: '–10:30', heading: 'ขายห้องแบบไหน ให้แขกกลุ่มไหน', body: 'เริ่มจากปัญหาหน้างานและห้องจริง 1 ประเภท ด้วย DARUMA Mini-Score แล้วแปลงจุดเด่นของที่พักให้เชื่อมกับสิ่งที่แขกอยากได้จริง ไม่ใช่ท่องรายการสิ่งอำนวยความสะดวก', output: 'FFAB Sheet รายประเภทห้อง' },
    { time: '10:45', until: '–12:00', heading: 'สร้างสมองโรงแรมด้วย Context + Data', body: 'รวมประเภทห้อง ราคาแต่ละฤดู แพ็กเกจ เงื่อนไขการจองและยกเลิก ให้เป็นไฟล์กลางของโรงแรม แล้วเทียบให้เห็นจะจะว่าคำตอบของ AI ก่อนกับหลังมี context ต่างกันแค่ไหน', output: 'company-context.md ของโรงแรม' },
    { time: '13:00', until: '–14:45', heading: 'สร้าง Specialist Agent คนละ 1 ตัว', body: 'แต่ละคนเลือกหน้าที่เดียวจาก Starter Kit เช่น ร่างตอบแชทแขกต่างชาติ หรือทำใบเสนอราคากรุ๊ป ปรับด้วยข้อมูลที่พัก แล้วทดสอบกับข้อความแขกจริงของตัวเอง', output: 'Specialist Agent ของแต่ละคน' },
    { time: '15:00', until: '–16:25', heading: 'คุมและใช้ต่อ แล้วแต้มตาดารุมะ', body: 'จับผิดงานที่ AI ทำพลาดด้วยกัน กำหนดว่าราคาและเงื่อนไขยกเลิกต้องมีคนตรวจก่อนส่งเสมอ แล้วล็อกงานแรก วันเวลา และคนรับผิดชอบลง Monday Plan', output: 'Review Checklist + Monday Plan', final: true },
  ],
  nudgeAgenda: 'อ่านตารางแล้วยังนึกไม่ออกว่าทีมคุณจะทำอะไรในช่วงบ่าย? ทักมาถามได้ครับ ผมเล่าให้ฟังทีละช่วง',

  menuTitle: 'แต่ละคนเลือก agent ตามงานที่ตัวเองรับผิดชอบ',
  menuLead: 'agent หน้าที่เดียวคุมง่ายกว่า agent ที่พยายามทำทุกอย่าง และหัวหน้าตรวจได้ว่ามันควรรับงานตรงไหน',
  menu: [
    { heading: 'ตัวร่างตอบแชทแขก ไทยและอังกฤษ', body: 'จับประเด็นจากข้อความแขกแล้วเตรียมร่างให้ทีมตรวจ ก่อนตอบกลับ' },
    { heading: 'ตัวคัดกรองการจอง', body: 'ถามข้อมูลที่ต้องรู้ให้ครบ วันเข้าพัก จำนวนคน ประเภทห้อง ก่อนส่งต่อให้ Reservation' },
    { heading: 'ตัวช่วยร่างใบเสนอราคากรุ๊ป', body: 'ร่างจากราคาและแพ็กเกจจริงของที่พัก แล้วให้คนตรวจก่อนส่งลูกค้าองค์กรทุกครั้ง' },
    { heading: 'ตัวสรุปรีวิวและข้อร้องเรียน', body: 'อ่านรีวิวย้อนหลังแล้วบอกว่าเรื่องไหนพังซ้ำๆ ก่อนดาวจะตก' },
    { heading: 'Product Specialist ของที่พัก', body: 'ตอบเรื่องห้อง สิ่งอำนวยความสะดวก และกิจกรรม จากข้อมูลที่โรงแรมอนุญาต' },
  ],

  takeHomeTitle: 'จบวันแล้ว ทีมไม่ได้ถือ "ความรู้" กลับไปอย่างเดียว',
  takeHomeFeature: { heading: 'Specialist AI Agent คนละ 1 ตัว', body: 'สร้างเสร็จในห้อง ทดสอบกับข้อความแขกจริงแล้ว พร้อมใช้ต่อกะถัดไป ทุกชิ้นต่อกันเป็น "สมองโรงแรม" เวอร์ชันแรกของคุณ' },
  takeHome: [
    { heading: 'company-context.md ของโรงแรม', body: 'ไฟล์กลางที่รวมประเภทห้อง ราคาแต่ละฤดู แพ็กเกจ และเงื่อนไขการจอง' },
    { heading: 'FFAB Sheet รายประเภทห้อง', body: 'จุดเด่นของห้องเชื่อมกับสิ่งที่แขกแต่ละกลุ่มมองหาจริง' },
    { heading: 'คลัง prompt งานโรงแรม 23+ ตัว', body: 'ไทยและอังกฤษ ปรับเข้ากับที่พักคุณระหว่างคลาส ใช้ต่อได้ทันที' },
    { heading: 'Review Checklist + Data Rules', body: 'กติกาว่าราคาและเงื่อนไขยกเลิกต้องผ่านคนตรวจก่อนเสมอ' },
    { heading: 'Monday Plan รายคน', body: 'งานแรก วันเวลา และคนรับผิดชอบ กันโรค "อบรมเสร็จแล้วเงียบ"' },
  ],
  takeHomeNote: 'พร้อมติดตามผ่าน LINE 7 วันหลังเรียน ผมไม่รับประกันยอดจองนะครับ ผลขึ้นกับข้อมูลและวินัยของทีม แต่ของที่ต้องใช้ต่อ ผมวางให้ครบก่อนจบวัน',

  fitTitle: 'คอร์สนี้เจาะจงกับที่พักที่อยากเปลี่ยน "ของที่เรียน" ให้เป็น "ของที่ใช้"',
  fitYes: [
    'เจ้าของโรงแรมหรือรีสอร์ท ที่มีทีม Front และ Reservation รวมกัน 5 คนขึ้นไป',
    'ผู้จัดการทั่วไปที่อยากให้ทุกคนตอบแขกด้วยข้อมูลชุดเดียวกัน ไม่ใช่ตอบตามที่จำได้',
    'ทายาทหรือผู้บริหารรุ่นใหม่ ที่ต้องเอาแผนกับของจริงกลับไปคุยกับที่บ้านให้ผ่าน',
  ],
  fitNo: 'ที่พักที่ยังไม่มีทีมประจำ · อยากได้คอร์ส prompt กว้างๆ · อยากได้ระบบจองอัตโนมัติเต็มรูปแบบในวันเดียว · ยังไม่มีข้อมูลห้องพักและราคาให้ใช้ในคลาส',
  nudgeFit: 'อ่านแล้วยังไม่ชัดว่าที่พักคุณเข้าข่ายไหม? เล่าให้ผมฟังสั้นๆ ในไลน์ ผมบอกตรงๆ ว่าเหมาะหรือยังไม่เหมาะ',

  classPhotos: [
    { src: '/testimonial/2026-07/scenery/scenery-room.jpg', alt: 'คลาส in-house ที่ The Scenery สวนผึ้ง ทีมงานนั่งประจำโต๊ะพร้อมโน้ตบุ๊ก', width: 1100, height: 825 },
    { src: '/lp/inhouse/class-full.jpg', alt: 'คลาสอบรมเต็มห้อง ผู้เรียนนั่งประจำโต๊ะพร้อมโน้ตบุ๊ก', width: 1600, height: 1200 },
    { src: '/lp/inhouse/office-session.jpg', alt: 'อบรมในห้องประชุม ทีมดูสไลด์แล้วทำตามบนเครื่องตัวเอง', width: 1600, height: 1200 },
    { src: '/lp/inhouse/onsite-cafe.jpg', alt: 'เวิร์คช็อปหน้างานจริง ทีมนั่งทำงานกับเครื่องของตัวเอง', width: 1200, height: 1600 },
    { src: '/lp/inhouse/team-group.jpg', alt: 'ทีมผู้เรียนถ่ายรูปรวมกันในห้องอบรม พร้อมโน้ตบุ๊กเปิดค้างบนโต๊ะ', width: 1200, height: 1600 },
    { src: '/testimonial/web/fb-learning-org.jpg', alt: 'โพสต์จากองค์กรที่เข้าอบรม', width: 1080, height: 1080 },
  ],
  reviews: [
    { src: '/testimonial/2026-07/review-09.jpg', alt: 'แชทรีวิวจากผู้เรียนจริง', width: 1410, height: 1410 },
    { src: '/testimonial/2026-07/review-10.jpg', alt: 'แชทรีวิวจากผู้เรียนจริง', width: 736, height: 736 },
    { src: '/testimonial/2026-07/review-11.jpg', alt: 'แชทรีวิวจากผู้เรียนจริง', width: 914, height: 914 },
    { src: '/testimonial/2026-05/review-02.jpg', alt: 'แชทรีวิวจากผู้เรียนจริง', width: 1000, height: 1000 },
    { src: '/testimonial/2026-05/review-04.jpg', alt: 'แชทรีวิวจากผู้เรียนจริง', width: 1000, height: 1000 },
    { src: '/testimonial/2026-05/review-05.jpg', alt: 'แชทรีวิวจากผู้เรียนจริง', width: 1000, height: 1000 },
  ],

  instructorPhoto: { src: '/lp/inhouse/pun-portrait.jpg', alt: 'ปัน ณัฐพัชร์ ผู้สอนคอร์ส Advance AI Agent สำหรับโรงแรม', width: 1066, height: 1600 },
  instructorParas: [
    'ผมเป็นคนขายมาก่อนครับ เคยยืนรอลูกค้า เคยโดนเทตอนใกล้ปิด เคยนั่งทำเอกสารเสนอราคาถึงเที่ยงคืน เพราะงั้นเวลาสอน ผมรู้ว่างานไหน "พูดง่ายแต่ทำจริงเหนื่อย"',
    'ทุกระบบ AI ที่สอนในคลาส ผมสร้างใช้เองในธุรกิจตัวเองก่อนทั้งหมด ตั้งแต่ตามลูกค้า ตอบแชท ทำเอกสารขาย ไปจนถึงหลังบ้านของเพจที่คุณกำลังอ่านอยู่นี่แหละครับ',
    'หน้าที่ของผมในวันอบรมคือเดินดูหน้าจอทีละคน ช่วยจัด context และถามคำถามให้ agent ของแต่ละคนผูกกับงานหน้าเคาน์เตอร์จริง ผมอยู่ในห้องเพื่อพาทีมทำ ไม่ได้อยู่บนเวทีเพื่อเล่าเรื่องตัวเอง',
    'ผมไม่ใช่คนที่เก่ง AI ที่สุดในประเทศหรอกครับ แต่ผมน่าจะเป็นคนที่นั่งอยู่ตรงกลางระหว่าง "ทีมหน้างานจริง" กับ "ของพวกนี้" มานานพอที่จะแปลให้สองฝั่งเข้าใจกันได้',
  ],

  priceLabel: 'Advance AI Agent · In-house สำหรับโรงแรม',
  priceAmount: fmtPrice('inhouse-a'),
  priceNote: `มากัน 20 คน เฉลี่ยคนละ ${perHead.toLocaleString('th-TH')} บาท ถูกกว่าส่งพนักงานไปเรียนข้างนอก 1 ที่นั่ง`,
  priceIncludes: [
    'อบรม on-site ที่โรงแรมหรือรีสอร์ทของคุณ 1 วันเต็ม (ทีม 5–20 คน)',
    'Specialist AI Agent คนละ 1 ตัว สร้างจากข้อมูลห้องพักจริง เสร็จในวันนั้น',
    'สมองโรงแรม: company-context.md + FFAB Sheet + prompt 23+ ตัว',
    'Review Checklist + Data Rules + Monday Plan รายคน',
    'ติดตามผ่าน LINE 7 วันหลังเรียน',
    'ใบกำกับภาษีนามบริษัท ฝ่ายบัญชีเบิกได้',
  ],
  priceTerms: [
    'เงื่อนไขเดียวที่ผมขอ: ชำระเต็มจำนวนก่อนวันอบรมครับ เพื่อให้ทั้งสองฝั่งจริงจังกับวันนั้นเท่ากัน · on-site กรุงเทพฯ และปริมณฑล ไม่คิดค่าเดินทาง ต่างจังหวัดคุยกันได้',
    'อยากให้ผมอยู่ยาวกว่า 7 วัน มีแพ็กเกจ "พาลงมือทำ" ต่อยอดหลังคอร์ส ถามรายละเอียดได้ในคิวคุยฟรี',
  ],
  priceReassure: 'ยังไม่ต้องเชื่อผมครับ ให้ผมดูงานหน้าเคาน์เตอร์ของคุณก่อน แล้วค่อยตัดสินใจว่าคุ้มไหม',

  faqs: [
    { q: 'ทีมผมไม่เก่งเทคโนโลยีเลย เรียนไหวไหม', a: 'ไหวครับ ไม่ต้องเขียนโค้ด สั่งงานเป็นภาษาไทยได้ และเราเริ่มจาก template ไม่ใช่หน้าจอว่างๆ ทุกขั้นตอนทำตามพร้อมกันทีละคลิก ใครหลุดผมเดินไปนั่งข้างๆ จนตามทัน คนที่พิมพ์ช้าที่สุดในห้องคือ KPI ของผม' },
    { q: 'ต้องเตรียมอะไรก่อนวันอบรม', a: 'เตรียมข้อมูลประเภทห้อง ราคาแต่ละฤดู แพ็กเกจ เงื่อนไขการจอง และตัวอย่างแชทแขกจริง ตาม Pre-work Checklist ที่ผมส่งให้หลังยืนยันวัน แล้วให้ IT เช็คการเข้าถึงเครือข่ายก่อน เพื่อให้เวลาในห้องหมดไปกับการลงมือทำ' },
    { q: 'ข้อมูลราคากับข้อมูลแขก ปลอดภัยไหม', a: 'ในคลาสมี Data Rules ชัดเจนครับ ข้อมูลส่วนตัวแขก เลขบัตร ข้อมูลการชำระเงิน และสัญญาที่โรงแรมไม่อนุญาต ต้องตัดออกหรือทำให้ไม่ระบุตัวตนก่อนใช้ และกติกานี้เป็นหนึ่งในของที่ทีมได้กลับไปใช้ต่อด้วย' },
    { q: 'AI จะตอบแขกแทนทีมเลยไหม', a: 'ไม่ครับ และผมพูดเรื่องนี้ตรงๆ ในคลาสด้วย agent มีหน้าที่ร่าง คัดกรอง สรุป และเตรียมข้อมูล ส่วนราคาและเงื่อนไขยกเลิกต้องมีคนตรวจและอนุมัติทุกครั้ง งานบริการยังเป็นงานของคน' },
    { q: 'จบคลาสแล้ว ถ้าติดปัญหาถามใครได้', a: 'มีติดตามผ่าน LINE 7 วันหลังเรียนครับ อาทิตย์แรกคือช่วงที่คำถามจริงโผล่ ผมตอบเองทั้งหมด ถ้าอยากได้คนพาไปต่อยาวกว่านั้น มีแพ็กเกจ "พาลงมือทำ" ที่ผมเข้าไปทำงานกับทีมต่อเนื่อง คุยกันได้ครับ' },
    { q: 'ทีมเข้ากะกันคนละเวลา จัดยังไงดี', a: 'คุยกันก่อนได้ครับ ส่วนใหญ่ที่ทำกันคือเลือกวันที่ occupancy ต่ำแล้วรวมทีมให้ได้มากที่สุดในรอบเดียว ถ้าจำเป็นต้องแบ่ง 2 รอบ ผมจัดให้ได้ แต่ราคาจะคิดตามจำนวนวันจริง' },
    { q: 'จ่ายเงินยังไง มีเอกสารให้ฝ่ายบัญชีไหม', a: 'ชำระเต็มจำนวนก่อนวันอบรม ออกใบกำกับภาษีในนามบริษัทให้ครบครับ ใบเสนอราคาสำหรับขออนุมัติภายในก็ทำให้ได้ บอกผมในคิวคุยฟรีได้เลย' },
  ],
  nudgeFaq: 'คำถามของคุณไม่อยู่ในลิสต์นี้ใช่ไหมครับ? ถามผมตรงๆ ในไลน์ได้เลย ไม่ต้องกรอกฟอร์ม',
};
---

<BaseLayout
  title={title}
  description={description}
  canonical={canonical}
  ogImage={`${SITE.url}/og-image.jpg`}
  noindex
  robots="noindex,follow"
  schema={{ types: ['WebPage'], title, description }}
>
  <link
    slot="head"
    rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Bai+Jamjuree:wght@500;600;700&display=swap"
  />

  <div id="hotel-ai-page" class="lp-v2">
    <AdsContentV2 {...content} />

    <section id="hotel-ai-form" class="scroll-mt-20 px-6 py-16 md:py-24">
      <div class="mx-auto grid max-w-6xl gap-10 lg:grid-cols-[.8fr_1.2fr] lg:items-start">
        <div>
          <p class="text-xs font-bold uppercase tracking-[0.18em] text-cyan">จองคิวคุยฟรี</p>
          <h2 class="mt-3 font-display text-3xl font-bold leading-tight text-navy md:text-5xl">เล่าโจทย์หน้างานที่โรงแรมให้ผมฟัง</h2>
          <p class="mt-5 leading-relaxed text-warm-ink/75">กรอกข้อมูลสั้นๆ ไว้ครับ เดี๋ยวผมทักกลับทาง LINE หรือโทรศัพท์ แล้วคุยกัน 15–20 นาที ว่าที่พักของคุณควรเริ่มวางระบบ AI ตรงไหนก่อน</p>
        </div>

        <form id="hotel-ai-booking-form" class="editorial-card p-6 md:p-9" novalidate>
          <input name="honeypot-name" tabindex="-1" autocomplete="off" class="sr-only" aria-hidden="true" />
          <div class="space-y-5">
            <label class="field-label">ชื่อของคุณ<input name="name" required autocomplete="name" class="field" aria-label="ชื่อของคุณ" /></label>
            <label class="field-label">โรงแรม / รีสอร์ทของคุณชื่ออะไรครับ<input name="business" required autocomplete="organization" class="field" aria-label="โรงแรม หรือ รีสอร์ทของคุณชื่ออะไร" /></label>
            <fieldset class="field-label">
              <legend class="mb-2">อยากให้ผมติดต่อกลับแบบไหนครับ</legend>
              <div class="grid gap-3 sm:grid-cols-2">
                <label class="intent-option flex cursor-pointer items-start gap-3 rounded-xl border border-[#c9c2b2] bg-ivory p-4">
                  <input type="radio" name="intent" value="chat" required class="mt-1 accent-coral" aria-label="คุยเบื้องต้นก่อน" />
                  <span><span class="block font-semibold text-navy">คุยเบื้องต้นก่อน</span><span class="mt-0.5 block text-sm font-normal text-warm-ink/70">อยากสอบถามรายละเอียดก่อน</span></span>
                </label>
                <label class="intent-option flex cursor-pointer items-start gap-3 rounded-xl border border-[#c9c2b2] bg-ivory p-4">
                  <input type="radio" name="intent" value="booking" required class="mt-1 accent-coral" aria-label="ขอนัดจองคิวเลย" />
                  <span><span class="block font-semibold text-navy">ขอนัดจองคิวเลย</span><span class="mt-0.5 block text-sm font-normal text-warm-ink/70">พร้อมนัดคุยจริงจัง</span></span>
                </label>
              </div>
            </fieldset>
            <label class="field-label">เบอร์โทรมือถือ<input name="phone" type="tel" required inputmode="tel" autocomplete="tel" pattern={"[0-9\\(\\)\\+\\-\\s]{9,15}"} class="field" placeholder="เช่น 0812345678" aria-label="เบอร์โทรมือถือ" /></label>
            <div class="grid gap-5 sm:grid-cols-2"> <label class="field-label">ตำแหน่งของคุณ<select name="position" required class="field" aria-label="ตำแหน่งของคุณ"><option value="">เลือกตำแหน่ง</option><option>CEO / เจ้าของกิจการ</option><option>Managing Director</option><option>Sales Director</option><option>Sales Manager</option><option>Marketing Manager</option><option>Operations Manager</option><option>อื่นๆ</option></select></label> <label class="field-label">LINE ID <span class="font-normal text-warm-ink/55">(ถ้ามี)</span><input name="line_id" autocomplete="off" class="field" placeholder="เช่น @yourhotel หรือ ไอดีไลน์" aria-label="LINE ID" /></label> </div>
            <label class="field-label">โจทย์หน้างานที่อยากแก้ <span class="font-normal text-warm-ink/55">(ถ้ามี)</span><textarea name="sales_issue" rows="4" class="field resize-y" aria-label="โจทย์หน้างานที่อยากแก้"></textarea></label>
            <input id="utm_source" name="utm_source" type="hidden" />
            <input id="utm_medium" name="utm_medium" type="hidden" />
            <input id="utm_campaign" name="utm_campaign" type="hidden" />
            <input id="utm_content" name="utm_content" type="hidden" />
            <input id="utm_term" name="utm_term" type="hidden" />
            <input id="fbclid" name="fbclid" type="hidden" />
            <input id="ttclid" name="ttclid" type="hidden" />
            <label class="flex cursor-pointer items-start gap-3 pt-1"><input name="consent" type="checkbox" required class="checkbox mt-0.5" aria-label="ยินยอมให้ติดต่อกลับตามนโยบายความเป็นส่วนตัว" /><span class="text-sm leading-relaxed text-warm-ink/75">ยินยอมให้ติดต่อกลับตาม <a href="/privacy" class="font-semibold text-cyan underline">นโยบายความเป็นส่วนตัว</a></span></label>
            <p id="hotel-ai-error" class="hidden rounded-xl border border-error/40 bg-error/10 p-4 text-sm text-error" role="alert" aria-live="polite"></p>
            <button id="hotel-ai-submit" type="submit" class="btn min-h-11 w-full border-0 bg-coral py-4 text-base text-white hover:bg-[#c43245]" aria-label="ส่งข้อมูล">ส่งข้อมูล</button>
          </div>
        </form>

        <div id="hotel-ai-success" class="editorial-card hidden p-8 text-center lg:col-start-2" role="status" aria-live="polite">
          <DarumaMark class="mx-auto h-auto w-20" />
          <h2 class="mt-5 font-display text-3xl font-bold text-navy">รับข้อมูลแล้วครับ</h2>
          <p class="mt-3 leading-relaxed text-warm-ink/75">ผมจะอ่านโจทย์ก่อนครับ แล้วทักกลับทาง LINE หรือโทรศัพท์ คุยกันฟรี 15–20 นาที</p>
        </div>
      </div>

      <div class="mx-auto mt-12 max-w-6xl border-t border-[#c9c2b2] pt-10">
        <p class="text-center text-lg font-semibold text-navy">ยังไม่อยากกรอกฟอร์ม? ทักไลน์มาถามผมสั้นๆ ก่อนก็ได้ครับ</p>
        <div class="mx-auto mt-5 flex max-w-2xl justify-center">
          <a href={lineUrl} data-line-hotel target="_blank" rel="noopener" class="line-button flex min-h-11 items-center justify-center gap-2 rounded-xl bg-[#06c755] px-4 py-3 text-center font-semibold text-white transition-colors hover:bg-[#05b34c]" aria-label="ทัก LINE ถามว่าที่พักผมเอา AI ไปใช้ได้ไหม">
            <svg class="size-5 shrink-0" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path fill="currentColor" d="M21.5 11.1c0-4.6-4.8-8.3-10.8-8.3S0 6.5 0 11.1c0 4.1 3.8 7.5 8.9 8.2-.2.2-.4.5-.5.8-.2.5-.1.9.4 1 .4.1.8-.1 1.2-.3l3.3-2.1c4.6-.8 8.2-3.7 8.2-7.6Z"/></svg>
            <span>ทัก LINE ถามว่าที่พักผมเอา AI ไปใช้ได้ไหม</span>
          </a>
        </div>
      </div>
    </section>
  </div>

</BaseLayout>


<style>
  /* Sea-and-shore layer for this page only. The dealer page stays flat ivory;
     here the light sections sit on a faint horizon wash and the dark sections
     carry palm silhouettes, so the two campaigns never read as the same page. */
  .hero-shore {
    background:
      linear-gradient(180deg, #f4f1ea 0%, #f2f2ec 55%, #e8eeee 100%);
  }
  .palm {
    position: absolute;
    pointer-events: none;
    color: #04283f;
    opacity: .05;
    width: 26rem;
  }
  .palm :global(svg) { width: 100%; height: auto; display: block; }
  .palm-hero { right: -7rem; bottom: -5rem; color: #0e7490; opacity: .10; }
  .palm-left  { left: -9rem;  top: -3rem;  color: #7dd3e8; opacity: .07; transform: scaleX(-1); }
  .palm-right { right: -9rem; bottom: -4rem; color: #7dd3e8; opacity: .07; }
  @media (max-width: 767px) { .palm { width: 17rem; } }

  /* The rail carries time, not decoration: left edge is midnight, right edge is
     midnight again, and the cards below sit in the order they happen. */
  .sky-rail {
    height: .5rem;
    border-radius: 999px;
    background: linear-gradient(90deg,
      #04283f 0%, #0b3f5c 14%, #2c7f96 26%, #cfe3e6 38%,
      #f4f1ea 50%, #ece6da 64%, #c98b6b 76%, #0b3f5c 88%, #04283f 100%);
  }

  .field-label { display: grid; gap: .45rem; color: var(--pun-navy); font-size: .95rem; font-weight: 600; }
  .intent-option { transition: border-color .15s, background .15s, box-shadow .15s; }
  .intent-option:has(input:checked) { border-color: var(--pun-coral); background: color-mix(in oklch, var(--pun-coral) 7%, transparent); box-shadow: inset 0 0 0 1px var(--pun-coral); }
  .field { width: 100%; border: 1px solid var(--pun-border); border-radius: .75rem; background: var(--pun-ivory); color: var(--pun-ink); padding: .75rem 1rem; min-height: 44px; }
  .field:focus { border-color: var(--pun-coral); outline: 2px solid color-mix(in oklch, var(--pun-coral) 25%, transparent); outline-offset: 2px; }
  .line-button { min-height: 44px; }
  :global(body:has(#hotel-ai-page) .nav-cta),
  :global(body:has(#hotel-ai-page) .menu-mobile li.mt-2) { display: none; }
  @media (prefers-reduced-motion: reduce) {
    .line-button { transition: none; }
  }
</style>

<script>
  type Attribution = Record<string, string | undefined>;
  type MetaWindow = Window & { fbq?: (...args: unknown[]) => void; ttq?: { track: (...args: unknown[]) => void }; pnAttribution?: Attribution };

  const pageWindow = window as MetaWindow;
  const form = document.getElementById('hotel-ai-booking-form') as HTMLFormElement | null;
  const formSection = document.getElementById('hotel-ai-form');
  const success = document.getElementById('hotel-ai-success');
  const error = document.getElementById('hotel-ai-error');
  const submit = document.getElementById('hotel-ai-submit') as HTMLButtonElement | null;
  const WEBHOOK = 'https://rnat.app.n8n.cloud/webhook/intake-form-v2';

  /**
   * Reference token, same shape as intake-form.astro's makeReference().
   * Added 2026-08-01: the ads landing pages never sent one, so `reference`
   * arrived empty at n8n — which meant the TikTok Events API node posted
   * event_id: "" on every ads lead and could not dedupe against the browser
   * pixel. It is also the event_id Meta CAPI will need.
   */
  function makeAdsReference(): string {
    const stamp = new Date().toISOString().replace(/[-:T.Z]/g, '').slice(0, 12);
    const rand = Math.random().toString(36).slice(2, 6).toUpperCase();
    return `PN-${stamp}-${rand}`;
  }

  const UTM_KEYS = ['utm_source', 'utm_medium', 'utm_campaign', 'utm_content', 'utm_term'];
  // Click IDs are the strongest match signal for Meta/TikTok server-side events.
  // BaseLayout already stashes them in pnAttribution (14-day TTL); before
  // 2026-08-01 the ads forms simply never read them back out, so every ads lead
  // reached the CRM with fbclid/ttclid empty.
  const CLICK_ID_KEYS = ['fbclid', 'ttclid'];

  function populateAttribution() {
    const query = new URLSearchParams(window.location.search);
    const stored = pageWindow.pnAttribution || {};
    [...UTM_KEYS, ...CLICK_ID_KEYS].forEach((key) => {
      const input = document.getElementById(key) as HTMLInputElement | null;
      if (input) input.value = query.get(key) || stored[key] || '';
    });
  }

  populateAttribution();

  if (formSection && 'IntersectionObserver' in window) {
    let fired = false;
    const observer = new IntersectionObserver((entries) => {
      if (entries.some((entry) => entry.isIntersecting) && !fired) {
        fired = true;
        if (typeof pageWindow.fbq === 'function') fbq('track', 'ViewContent', { content_name: 'hotel_ai_form' });
        observer.disconnect();
      }
    }, { threshold: 0.4 });
    observer.observe(formSection);
  }

  document.querySelectorAll('[data-line-hotel]').forEach((lineButton) => {
    lineButton.addEventListener('click', () => {
      if (typeof pageWindow.fbq === 'function') fbq('track', 'Contact', { content_name: 'line_add_hotel' });
      if (typeof pageWindow.ttq?.track === 'function') pageWindow.ttq.track('Contact', { content_name: 'line_add_hotel' });
    });
  });

  form?.addEventListener('submit', async (event) => {
    event.preventDefault();
    if (!form.reportValidity()) return;
    if ((new FormData(form).get('honeypot-name') as string)?.trim()) return;

    const phoneRaw = String(new FormData(form).get('phone') || '').replace(/[\s()\-]/g, '');
    if (!/^(?:\+?66|0)\d{8,9}$/.test(phoneRaw)) {
      if (error) { error.textContent = 'กรุณากรอกเบอร์โทรมือถือจริง เช่น 0812345678 ครับ'; error.classList.remove('hidden'); }
      (form.querySelector('[name="phone"]') as HTMLInputElement | null)?.focus();
      return;
    }

    const payload = Object.fromEntries(new FormData(form).entries());
    const intentLabel = payload.intent === 'booking' ? 'ขอนัดจองคิว' : 'คุยเบื้องต้นก่อน';
    Object.assign(payload, {
      phone: phoneRaw,
      lead_temp: payload.intent === 'booking' ? 'hot' : 'warm',
      // Compatibility aliases so the shared intake-form-v2 flow + submit_lead RPC
      // map these into CRM columns (it reads company/tier/problems/comment, not the raw dealer field names).
      company: payload.business || '',                        // -> company_name
      tier: payload.intent === 'booking' ? 'HOT' : 'WARM',    // -> temperature (hot/warm)
      problems: (payload.sales_issue as string) || '',        // -> pain_points
      comment: [intentLabel, payload.sales_issue].filter(Boolean).join(' — '), // -> crm_notes + Telegram alert
      source: 'ads-hotel-resort-ai',
      source_page: 'ads/hotel-resort-ai',
      booking_type: 'hotel-resort-ai',
      // Flatten Body1 reads `submittedAt` (camelCase) — the snake_case key used
      // here until 2026-08-01 never matched, so n8n silently fell back to its
      // own execution time.
      submittedAt: new Date().toISOString(),
      // Flatten Body1 sources reference/user_agent/referrer from `_meta` only.
      _meta: {
        reference: makeAdsReference(),
        page: window.location.pathname,
        user_agent: navigator.userAgent,
        referrer: document.referrer || '',
      },
    });

    if (submit) { submit.disabled = true; submit.textContent = 'กำลังส่ง...'; }
    error?.classList.add('hidden');

    try {
      const response = await fetch(WEBHOOK, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
        keepalive: true,
      });
      if (!response.ok) throw new Error('submission failed');
      if (typeof pageWindow.fbq === 'function') fbq('track', 'Lead', { content_name: 'hotel_ai_booking', lead_temp: payload.lead_temp });
      if (typeof pageWindow.ttq?.track === 'function') pageWindow.ttq.track('SubmitForm', { content_name: 'hotel_ai_booking', lead_temp: payload.lead_temp });
      form.classList.add('hidden');
      success?.classList.remove('hidden');
      success?.scrollIntoView({ behavior: 'smooth', block: 'center' });
    } catch {
      if (error) { error.textContent = 'ระบบรับข้อมูลขัดข้องชั่วคราว กรุณาลองใหม่ หรือแอด LINE เพื่อส่งโจทย์ให้ผม'; error.classList.remove('hidden'); }
      if (submit) { submit.disabled = false; submit.textContent = 'ส่งข้อมูล'; }
    }
  });

</script>
